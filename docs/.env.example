# Environment Variables Example
# Copy this file to .env and update with actual values
# Contact repository owner (@racho8) for the actual values

# Required: Backend microservice endpoint
MICROSERVICE_URL=<CONTACT_REPOSITORY_OWNER_FOR_URL>

# Optional: Server port (defaults to 8080)
PORT=8080

# Optional: Allowed CORS origin (for web client)
#ALLOWED_ORIGIN=https://claude.ai
ALLOWED_ORIGIN=*

# Google Cloud Configuration (for deployment)
GCP_PROJECT_ID=<CONTACT_REPOSITORY_OWNER_FOR_PROJECT_ID>

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Contact @racho8 on GitHub for actual values
# 3. Update .env with provided values
# 4. The .env file is ignored by git for security

# Security Note:
# Never commit actual credentials to git!
# See SECURITY.md for detailed security guidelines.
